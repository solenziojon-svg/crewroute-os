/** @type {import('next').NextConfig} */
const nextConfig = {
  // You can add custom config here later if needed
  // For now this helps Vercel detect the project correctly
};

export default nextConfig;